Canonical Master Beta — Phase 19C

Adds a separate Canonical Master Beta toolbar control. It runs a full two/three
track timeline using the sealed Master PlanSet, JIT incoming deck scheduling,
two-source budget, and session-bus Stop. Normal Play is unchanged; Lite disabled;
Seek disabled in beta.

Use only after Audit Master Plans shows every pair PASS. Test uninterrupted
completion first, then Stop before A->B, during A->B, between transitions, and
during B->C.
