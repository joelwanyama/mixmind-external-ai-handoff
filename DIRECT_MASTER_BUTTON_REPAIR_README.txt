Direct Master Button Repair

The attached real E:\job\mixmind\index.html was confirmed to still contain the
old button handler. This repair edits that exact file in place and creates a
backup before changing it.

1. Copy BOTH files from this ZIP directly into E:\job\mixmind:
   APPLY_DIRECT_MASTER_BUTTON_FIX.bat
   APPLY_DIRECT_MASTER_BUTTON_FIX.ps1
2. Double-click APPLY_DIRECT_MASTER_BUTTON_FIX.bat.
3. A black window must say:
   SUCCESS: Updated the real index.html beside this script.
4. Restart MixMind with RESET_AND_START_MIXMIND_WINDOWS.bat.
5. Ctrl+Shift+R in Edge.
6. Test Master Plan will now call the direct canonical Master renderer.

Do not manually copy index.html for this repair. The script updates the actual
file beside it and saves a backup first.
