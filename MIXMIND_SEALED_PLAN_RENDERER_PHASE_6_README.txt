MixMind Sealed Plan Renderer — Phase 6

This connects the test-only Lite Vocal-Safe Handoff v2 renderer to the exact
in-memory sealed test plan. It refuses to run unless the plan is SEALED, is a
LITE_CANDIDATE, contains a safe pair, and matches the current two-song timeline.

Normal Master playback is unchanged. This is the first controlled listening test
only. Do not refresh between sealing the plan and running it: the test plan is
stored in current browser memory.

Steps: install, Ctrl+Shift+R, Scan Lite Pairs, Seal Lite Test Plan, then click
Run Latest Sealed Lite Test in the scan panel. Listen only to the handoff once;
then report vocal overlap, clutter, timing, and control behavior.
