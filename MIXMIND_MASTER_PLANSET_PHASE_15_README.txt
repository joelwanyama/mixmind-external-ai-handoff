Master PlanSet Phase 15

Adds a timeline-wide sealed Master PlanSet foundation. Audit Master Plans now
builds one Master plan per adjacent pair, each with fingerprint, recipe,
effective seconds, fallback, source budget, and validation state. This remains
read-only: normal playback still does not consume the PlanSet.

Install, hard refresh, click Audit Master Plans. The panel now audits the
Master PlanSet rather than unrelated on-demand display data.
