Master PlanSet Schedule Phase 18

Adds a read-only global schedule to the sealed Master PlanSet. Audit Master
Plans now shows the global transition start for each sealed pair and computes the
PlanSet total duration from canonical effective overlaps. It creates no audio
sources and does not change normal playback.
