(function(){
function apply(){let changed=0;for(let i=0;i<timeline.length-1;i++){const key=i+'-'+(i+1);if(transitions[key]&&transitions[key]._manual===true)continue;const d=window.MixMindCanonicalTransition&&window.MixMindCanonicalTransition.build(i);if(!d||d.execution.mode!=='master'||!d.fallback||!d.fallback.recipe)continue;const current=getTransitionForPair(transitions,i,timeline[i],timeline[i+1],true);if(current.type===d.fallback.recipe&&Math.abs((current.duration||0)-d.timing.effectiveDuration)<.1)continue;transitions[key]={type:d.fallback.recipe,duration:d.timing.effectiveDuration,score:current.score,desc:'Safety fallback selected from canonical collision analysis.',reason:d.fallback.reason,_autoFallback:true,_forceMasterFallback:true};changed++;}if(changed){if(typeof renderTimeline==='function')renderTimeline();if(typeof simpleRefreshHome==='function')simpleRefreshHome();if(typeof showToast==='function')showToast('Applied '+changed+' safe Master fallback'+(changed===1?'':'s')+'.','success');}else if(typeof showToast==='function')showToast('No automatic transitions needed safe fallback changes.','success');}
window.applySafeMasterFallbacks=apply;
const old=window.renderTimeline;if(old)window.renderTimeline=function(){const r=old.apply(this,arguments);const bar=document.querySelector('#mixTab .toolbar');if(bar&&!bar.querySelector('.safe-fallbacks-btn')){const b=document.createElement('button');b.className='btn btn-sm safe-fallbacks-btn';b.textContent='Apply Safe Fallbacks';b.title='Replace unsafe automatic transitions with canonical Master fallbacks. Manual choices are preserved.';b.onclick=apply;bar.appendChild(b);}return r;};})();
