(function(){
function select(a,b,master,gate){const beat=Math.min(Number(a.analysisConfidence&&a.analysisConfidence.bpm)||0,Number(b.analysisConfidence&&b.analysisConfidence.bpm)||0),phrase=Math.min(Number(a.analysisConfidence&&a.analysisConfidence.sections)||0,Number(b.analysisConfidence&&b.analysisConfidence.sections)||0),key=Math.min(Number(a.analysisConfidence&&a.analysisConfidence.key)||0,Number(b.analysisConfidence&&b.analysisConfidence.key)||0),drift=gate&&Number(gate.drift)||0,keyRisk=1-(AIEngine.keyCompatibility(a.key,b.key)||0),noPair=gate&&gate.safePair&&!gate.safePair.valid,unsafeVocal=gate&&gate.reasons&&gate.reasons.some(x=>/vocal-safe|pre-vocal/i.test(x)),low=Math.max(Number(a.energy)||0,Number(b.energy)||0)>=8&&gate&&gate.safePair&&gate.safePair.incoming&&gate.safePair.incoming.low>.7;if(beat<.5||phrase<.5)return{recipe:'Hard Cut',effectiveDuration:.2,reason:'Low beat or phrase confidence makes a conservative downbeat cut safest.'};if(drift>.15)return{recipe:'Echo Out',effectiveDuration:1.5,reason:'Predicted BPM drift makes an overlapping blend unsafe; use a short reset transition.'};if(noPair||unsafeVocal)return{recipe:'Echo Out',effectiveDuration:1.5,reason:'No confirmed vocal-safe phrase window exists; use a short reset instead of a long blend.'};if(keyRisk>.55&&key<.65)return{recipe:'Echo Out',effectiveDuration:1.5,reason:'Harmonic confidence/risk is too weak for a long tonal overlap.'};if(low)return{recipe:'EQ Swap',effectiveDuration:Math.min(4,master.duration||4),reason:'Low-end overlap risk favors a short controlled master EQ handoff.'};return{recipe:master.type||'Crossfade',effectiveDuration:null,reason:'Master recipe remains safe for this pair.'};}
window.MixMindMasterFallbackSelector=Object.freeze({select});})();
